import styled from 'styled-components';

import Button from '@material-ui/core/Button';

const S = {
  NewNote: styled(Button)`
    && {
      color: white;
      width: 20%;
      background: black;
    }
  `,
};

export default S;