import styled from 'styled-components';

import Button from '@material-ui/core/Button';
import ExitToAppIcon from '@material-ui/icons/ExitToApp';

const S = {
  SignOutButton: styled(Button)``,
  ExitIcon: styled(ExitToAppIcon)``,
};

export default S;
