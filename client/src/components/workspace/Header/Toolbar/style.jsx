import styled from 'styled-components';

import Tabs from '@material-ui/core/Tabs';
import Tab from '@material-ui/core/Tab'
import SettingsIcon from '@material-ui/icons/Settings';

const S = {
  Tabs: styled(Tabs)`
  `,
  Tab: styled(Tab)`
  `,
  Settings: styled(SettingsIcon)`
  `,

};

export default S;
